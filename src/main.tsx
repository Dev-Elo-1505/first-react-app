import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import 'bootstrap/dist/css/bootstrap.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

/*
React is a javascript library unlike angular and vue that are javascript frameworks.
React alone is not enough to build apps, we need third party libraries
React can be used for mobile, web and desktop apps
React dom interacts with the DOm 
React native is used to build mobile apps*/
